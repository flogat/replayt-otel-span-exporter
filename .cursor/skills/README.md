# Cursor skills (optional)

Add project-specific [Cursor agent skills](https://docs.cursor.com/context/skills) as subfolders here. The repo’s
``.gitignore`` lists ``.cursor/skills/`` so this tree is not committed—keep skills local to each machine or team.
Remove this directory if you do not use Cursor.
